interface:
  display_name: "Any Sync"
  short_description: "Repo-specific patterns and workflows for any-sync"
  default_prompt: "Use the any-sync repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true